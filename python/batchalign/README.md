# batchalign

TalkBank CHAT processing pipeline. See `../../spec2.md` for the design
and `../../CLAUDE.md` for repo conventions.

Build the compiled extension:

```bash
cd python && maturin develop
```

Run the CLI:

```bash
batchalign3 --help
```
