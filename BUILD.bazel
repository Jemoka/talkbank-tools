"""Workspace root.

Tier discovery:

    //crates/batchalign/...   batchalign runtime + types + pyo3
    //apps/batchalign/...     React dashboard + Tauri shell
    //python/batchalign       Python product (maturin-wrapped wheel)
    //book                    mdBook documentation

Use fully qualified target paths directly, or the workspace-level `justfile`
(`just --list`) for convenience names.
"""

package(default_visibility = ["//visibility:public"])

exports_files([
    "Cargo.toml",
    "Cargo.lock",
])
